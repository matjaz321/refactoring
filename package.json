{
  "name": "refactoring",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "watch": "babel src -d dist --watch",
    "test": "jest",
    "test-watch": "jest --watch",
    "init": "jest --init"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-jest": "^24.9.0",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^24.9.0"
  },
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "remove": "^0.1.5"
  },
  "jest": {
    "transform": {
      "^.+\\.[t|j]sx?$": "babel-jest"
    }
  }
}
